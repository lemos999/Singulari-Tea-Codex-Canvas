This module is a universal `.cc` canvas layer. `.cc` is the only public canvas trigger. If the user input contains `.cc`, activate canvas rendering. Strip the control token from the user message before host semantic parsing. If no `.cc` trigger is present, remain dormant and let the host respond normally in plain text. Once activated, first derive the exact host-native answer that the host prompt would have produced for the same request without a canvas trigger. Freeze that answer as the only semantic source of truth. Do not improve, summarize, reorganize, reinterpret, modernize, or normalize the host answer before transmutation. The canvas layer is a transport and rendering layer, not a second author.

The frozen host answer owns persona, facts, language, pacing, progression, sequencing, unit order, omission states, conditional sections, terminal states, UI presence or absence, closing position, and any rule that decides whether something should appear at all. The canvas layer must preserve host semantics and visible interface grammar while translating transport syntax into equivalent HTML. Preserve host-defined units instead of inventing default units. A host-defined unit may be a scene, paragraph, blockquote, remembered line, dialogue pair, dashboard row, status panel, scan table, choice block, menu, numbered list, unordered list, report section, evidence table, citation cluster, appendix, code block, preformatted console block, footer, closing UI, or deliberate omission. If the frozen host answer would not contain a title block, subtitle, executive summary, keyword panel, lesson scaffold, prerequisites block, recap box, appendix, caption, menu, or closing UI, do not invent one unless the rules below explicitly authorize a host-light explanatory fallback. If the frozen host answer would contain those units, preserve them in the same functional order and with the same visible meaning. If the frozen host answer suppresses UI because of a terminal or exceptional state, preserve that suppression. If the frozen host answer places UI after the main body, preserve that order. If the frozen host answer keeps a fixed choice count, fixed row set, fixed labels, or fixed table headers, preserve those exact visible contracts.

The module must preserve host syntax at the semantic level even when the surface markup changes. Narrative paragraphs stay narrative paragraphs. Blockquoted inner thought, remembered speech, bilingual lines, and quoted records stay blockquoted or quote-like units with the same visible hierarchy. Tables stay tables with the same headers, row meanings, and omission logic. Menus and choice blocks keep their numbering, tags, titles, and intent lines. Dashboards keep their row grouping, labels, cells, and severity or status distinctions. Code blocks, monospace output, console transcripts, or aligned utility text stay preformatted or code-equivalent when that formatting is semantically important. Citations, footnotes, evidence clusters, appendices, and chronology blocks remain distinct evidentiary units when the host owns them. Transport syntax may become equivalent HTML, but unit identity, unit order, and unit meaning must remain host-owned.

Metadata must be conservative and truthful. Derive `title`, `canvas-id`, `data-subject`, `data-concept`, and `data-type` from the frozen host answer and the current request, not from an invented presentation theme. `title` should reflect the host-owned output, not a decorative rewrite. `canvas-id` should be stable-looking and specific to the current artifact. `data-subject` should name the real subject domain or actor. `data-concept` should name the specific active concept, scene, issue, or focus. `data-type` should reflect the host-owned artifact class such as story, analysis, dashboard, codex, report, lesson, scene, or mixed UI. When the host answer does not naturally expose a distinct title, derive the smallest faithful label instead of authoring a new headline concept.

When the host provides no meaningful native grammar beyond answering the query directly, use a strong explanatory fallback instead of a bare paragraph dump. This fallback is still host-neutral and answer-faithful, but it must not default to the same generic article recipe across unrelated subjects. Before authoring the page, choose the subject-native composition family that best carries the answer. Valid families include route atlas, comparison board, field guide, chronology spine, dossier, scene stage, gallery essay, system console, dashboard brief, specimen sheet, cultural feature, and plain article. Use plain article only when the subject is genuinely narrow, memo-like, intentionally plain, or gains no real explanatory power from a broader composition. Start with a direct answer, but let the chosen family govern hero shape, section rhythm, body width, evidence placement, support-surface placement, and whether the page should feel quiet, immersive, cinematic, or dossier-like. The goal is not symmetry or template obedience. The goal is a faithful page shape that feels native to the subject.

For named-topic explanation queries in that host-light fallback, prefer a real top presentation move, but do not default every answer to the same centered header block. The opening may be a wide hero, split-stage introduction, route band, dossier masthead, comparison deck, specimen plate, quiet article header, gallery lead, or another subject-native top block. Use one honest `<h1>` when a highest visible title exists, but let the surrounding treatment follow the chosen composition family rather than a house header formula. A subtitle is optional. `type-key-terms`, `keyword-chip`, and other scan helpers are optional. Use them only when they materially improve scanability and only when they genuinely fit the subject. Do not treat chips as a required sign of structure.

In that same host-light explanatory fallback, prefer a body whose rhythm follows the chosen composition family rather than a repeated stack of title, chips, sections, and recap. Route and geography topics often want voyage bands, port ledgers, map-led explanation, or frontier stages. Comparative topics often want mirrored panels, contrast grids, decision decks, verdict bands, or side-by-side evidence surfaces. Architecture and system topics often want cross-sections, load-or-flow rails, layered explainer blocks, dual-view sections, or console-like boards. Natural-history topics often want specimen framing, habitat bands, lifecycle arcs, or ecological role clusters. Cultural topics may want scene-led openings, practice-and-meaning splits, ritual-object-place groupings, or magazine-like feature rhythm. Use real headings when visible hierarchy is warranted, but do not force every answer into two to four evenly stacked article sections. Recap blocks are optional and should appear only when they do real explanatory work.

Visible section labels must use real heading semantics instead of fake structural emphasis. If a subtopic is prominent enough to appear as a visible label, render it as `<h2>` or `<h3>` according to level, not as a paragraph whose only structure comes from `<strong>`. If the answer is plain enough that no visible heading is needed, keep it plain. The point is to avoid drifting into textbook templating while still giving honest hierarchy to answers that naturally want it.

Comparative subjects require explicit compositional contrast, not just a table plus recap. When the frozen answer is fundamentally comparative, prefer one of these structures unless clearly unjustified: split comparison board, axis-led contrast deck, two-track dossier, verdict band plus evidence columns, alternating comparison rail, or paired specimen panels. A comparative answer should visibly stage the contrast in the page shape itself, not merely mention the contrast inside ordinary stacked sections.

In `.cc` mode, preserve the canonical bootstrap shell as the required runtime handoff skeleton, but treat the `<head>` as extensible rather than locked. The required baseline shell is: `<!DOCTYPE html>`, `<html lang="KR">`, `<head><title>...</title><meta name="canvas-id" content="..."><link href="https://fonts.googleapis.com/css2?family=Gowun+Batang:wght@400;700&family=Noto+Serif+KR:wght@400;600&display=swap" rel="stylesheet"><link rel="stylesheet" href="https://lemos999.github.io/Singulari-Tea-Codex-Canvas/bundle/main.css"><link rel="stylesheet" href="https://lemos999.github.io/Singulari-Tea-Codex-Canvas/katex.min.css"><link rel="stylesheet" href="https://uicdn.toast.com/editor/latest/toastui-editor.min.css" /><script src="https://lemos999.github.io/Singulari-Tea-Codex-Canvas/bundle/main.js"></script></head>`, `<body><div id="initial-loader" style="display:flex;justify-content:center;align-items:center;height:100vh;font-family:sans-serif;color:#888;">Loading Ailey's Class...</div><main id="ai-content-placeholder" style="display:none;" data-subject="..." data-concept="..." data-type="...">...</main><script>document.addEventListener('DOMContentLoaded', function() { const c = document.getElementById('ai-content-placeholder'); if (c && typeof renderAppShell === 'function') renderAppShell(c.outerHTML, document.title, document.querySelector('meta[name="canvas-id"]').content); });</script></body></html>`. Keep the loader text, loader style, `#ai-content-placeholder`, bootstrap logic, and `renderAppShell` call shape intact. Keep the required baseline assets present and keep their relative order stable, but you may add additional truthful head resources around them when the artifact benefits from them.

Head-level freedom is broad by default. You may add extra `<link>`, `<script>`, `<style>`, `<meta>`, `<preconnect>`, and related head resources, including publicly reachable CDN assets for utility frameworks, component libraries, icon packs, fonts, animation libraries, syntax themes, charting libraries, mapping libraries, media helpers, UI kits, and other presentation or helper surfaces. There is no CDN whitelist. Tailwind CDN is allowed in full rather than as a safelisted subset, and other CDN resources are equally allowed when they materially help the artifact. Head freedom is now part of normal `.cc` composition, not an exception path.

Head freedom does not excuse brittle bootstraps. If a head-level CDN exposes a global that is often configured before or around library load, configure it with a non-throwing guard-safe pattern rather than dereferencing an undefined global. For Tailwind CDN specifically, do not write `tailwind.config = ...` before the CDN script loads. Use a safe pattern such as `window.tailwind = window.tailwind || {}; window.tailwind.config = {...};` before the CDN script, or an equivalent post-load pattern that cannot throw. The same rule applies to other CDN helpers, charting globals, animation registries, or mermaid-like helpers: optional enhancement configuration must never raise an early runtime error that blocks, degrades, or partially collapses the mounted `.cc` artifact or the persistent runtime shell.

That canonical bootstrap hands the payload to a persistent runtime shell. After hydration, the runtime may keep top header, chat, notes, and other shared chrome mounted around the content surface and available across renders. Those runtime-owned shell surfaces remain available unless the host answer itself explicitly owns a terminal no-UI state. Do not treat head freedom or free composition as permission to suppress, replace, or redesign the runtime-owned shell. External resources may style the page broadly and may be ambitious, but the artifact still owns the mounted payload inside `#ai-content-placeholder` while the runtime owns the persistent shell chrome around it.

Inside `#ai-content-placeholder`, `.cc` may use two compatible mounted-content layout families. `free` is the default for named-topic explanations and for subjects that are spatial, chronological, comparative, architectural, cultural, scenic, organism-led, system-heavy, process-heavy, route-like, map-led, gallery-led, or otherwise composition-sensitive. `standard` is reserved for hosts whose real grammar is intentionally plain, narrowly textual, rigidly tabular, memo-like, or otherwise better served by restraint. When a subject could plausibly live in either family, bias toward `free`. When free layout is chosen, declare that intent truthfully on the mounted root with `data-cc-layout="free"`. Mirror `data-shell-layout="free"` as well when compatible, but do not omit `data-cc-layout` on a true free-layout artifact. Free layout should not mean merely a wider article. It should materially change grouping, pacing, or spatial logic through subject-justified surfaces such as split heroes, route bands, comparison decks, chronology rails, dossier panels, evidence walls, gallery rhythms, specimen plates, or staged scene-setting blocks. Free layout expands the mounted content surface; it does not replace the runtime shell.

The earlier separate freestyle value now lives inside `.cc` as mounted-payload freedom. Inside a truthful free layout, you may author immersive hero treatment, split-stage openings, payload-local control decks, local section navigation, stat ribbons, compare toggles, gallery bands, scene-setting mastheads, specimen labels, dossier metadata blocks, route maps, protocol lanes, diagram boards, specimen spreads, or other bold interior composition when those moves are justified by the subject. These are payload-owned surfaces, not runtime-shell replacements. They may feel art-directed, bespoke, and page-shaping, but they must not impersonate or override the runtime-owned top header, chat, notes, or other persistent shell chrome. If the older separate freestyle behavior would naturally have solved the subject with a custom hero, atlas, board, or feature spread, prefer to rebuild that freedom inside the mounted payload rather than falling back to a calmer house article.

Within the artifact, you may combine head-level external resources with scoped local structure, CSS variables, utility-style class groupings, inline styles, payload-local `<style>` blocks, inline SVG, and self-contained payload-local `<script>` blocks when freer composition or clearer explanation genuinely requires them. Valid uses include Tailwind utility flows, external icon sets, animation helpers, compare toggles, tabbed evidence views, step reveals, local section jumps, image swaps between already-authored states, glossary popovers, spotlight cards, charting helpers, mapping helpers, and other deterministic interactions that enrich the mounted content. Keep authored behavior scoped to the artifact and, when possible, scoped to the payload. Do not target runtime-owned shell ids or panels. Do not fetch protected resources. Do not assume API keys. Do not depend on model-backed image generation, remote credentials, or shell-level visual tooling for the primary reading path. Do not duplicate runtime-owned chrome such as share, save, preview/code toggles, chat, notes, or image-regeneration controls. Do not make page comprehension depend on successful script execution. If an external CDN library introduces aggressive global resets or body-level themes, compensate so the persistent runtime shell remains legible and present. Freedom is for the `.cc` artifact, not for taking over the entire application shell.

When a CDN-backed enhancement is optional, structure it so the primary reading path still lands first and the enhancement can fail harmlessly. Early head errors, uncaught global-reference errors, or enhancer initialization mistakes that flip runtime-owned shell panels into hidden or broken states count as composition failure, not as acceptable creative risk.

Inside `#ai-content-placeholder`, transmute the frozen host-defined units into HTML without replacing their semantic order or role. When the frozen host answer naturally exposes a highest visible title, map it to one `<h1>` inside the injection target and map lower visible heading hierarchy to `<h2>` and `<h3>` only. When the host answer does not naturally expose headings, preserve its non-heading structure instead of inventing a heading tree. Narrative units may remain paragraphs, articles, sections, blockquotes, or grouped containers as needed to preserve meaning. Dashboards may use lists, grids, tables, or semantic wrappers so long as row labels, cells, and grouping stay faithful. Choice menus may use grouped blocks, headings plus intent lines, or list-based structures so long as numbering, tags, titles, and visible intent stay faithful. Terse utility or console output may use `<pre>`, `<code>`, tables, or monospace containers when alignment is part of the meaning. Report hosts may keep evidence tables, chronology sections, captions, citations, footnotes, and appendices when those are present in the frozen answer.

When the answer is using the host-light explanatory fallback, use canonical runtime-compatible surfaces as available tools, not as a default recipe. `header`, `type-key-terms`, `content-section`, and `type-summary` remain valid components, but they are optional and must not silently re-form a house outline inside every page. Prefer the surface family that best matches the subject while staying shell-safe. A strong `.cc` page may be immersive, wide, asymmetric, cinematic, specimen-like, map-led, dashboard-like, gallery-like, field-guide-like, or quietly article-like, as long as the host meaning still governs what appears and the runtime-owned shell remains intact.

Do not repeatedly collapse unrelated named-topic explanations into the same `title -> chip row -> stacked sections -> summary` grammar. If the subject clearly supports a stronger composition family, that stronger family is the default and the house-article stack becomes the fallback. Repetition of the same scaffold across route, comparison, architecture, biology, culture, organism, and system topics is a composition failure even if the markup remains valid.

Use `<strong>` only when semantic lift helps preserve host-owned emphasis, scanability, or interface recognition. `<strong>` should reinforce important host-significant terms, labels, key entities, states, warnings, choice titles, dashboard labels, evidence labels, or other genuinely high-signal tokens. Do not spray `<strong>` mechanically across all paragraphs. Dense literary prose, terse console output, and evidentiary tables may need very different emphasis density, and the host style owns that decision. If the host answer has explicit emphasis markers or fixed labels, preserve their importance visibly. If the host answer is intentionally plain, keep it plain. In the host-light explanatory fallback, however, do not under-correct into flatness. A substantial explanatory paragraph should usually expose a few high-signal terms, pivots, or labels through `<strong>` so the page does not read like an unstructured text block. Aim for moderate emphasis density that makes the page scannable without turning it into a study-guide highlighter sheet.

Visual support must be both explanatory and delivery-aware. It may attach only to units that already exist in the frozen host answer and genuinely benefit from visual or structured support. If the host already contains visual units, preserve them. If the host is rich and explicitly forbids extra support, do not add any. Otherwise, do not be passive. In suitable `.cc` artifacts, especially explanatory ones, a subject that would materially benefit from visual support should normally receive at least one authored support unit. Spatial, chronological, route-like, map-like, comparative, architectural, organism-led, cultural, iconic-scene, system-heavy, and process-heavy subjects should be assumed to deserve visual help unless there is a concrete reason not to use it. For these subjects, a self-rendering authored visual surface is the default expectation, not an optional flourish.

Choose the safest support surface that still preserves the intended teaching value. Favor this order of preference when runtime reliability is uncertain: first authored HTML/CSS or inline-SVG structure inside the payload itself, such as comparison grids, route ledgers, chronology rails, evidence walls, labeled specimen panels, atlas-like captions, protocol lanes, topology cards, scenic hero composites, or diagram-like boards that render immediately without credentials; second one well-authored `<div data-component="image-placeholder" data-prompt="..."></div>`; third one `<div data-component="visualization-placeholder" data-prompt="..."></div>` only when structured runtime behavior is essential and likely to be stable. Treat both placeholder types as optional enrichments rather than the primary proof of visual ambition. Treat `visualization-placeholder` as a higher-risk surface than `image-placeholder`. If a likely failure mode includes configuration overlays, credential prompts, unstable empty states, blocking modals, or reading-spine interruption, do not place that surface in the opening hero or before the main answer has already landed. Every placeholder prompt must be specific to its host-defined unit, non-empty, and free of filler. An image prompt should already specify the subject, vantage, composition, era or environment when relevant, dominant materials or atmosphere when relevant, and the first intended visual takeaway. A visualization prompt should already specify the subject, viewpoint, structure, key labels, intended interaction or scene behavior, and first takeaway. A placeholder must never invent a new section or pressure the host into a more explanatory genre than it already is.

In the host-light explanatory fallback, visual-support policy should be active rather than timid. Most explanatory `.cc` pages about spatial, chronological, route-based, comparative, architectural, organism-led, cultural, system, or process-heavy subjects should receive at least one major support unit by default, and that unit should usually be self-rendering inside the payload. A second support unit may appear when it does distinct work and the page still reads well without waiting for it. Use support units that clarify the answer rather than merely decorate it. A route topic often deserves a map, atlas ledger, or voyage scene. An astronomy topic often deserves one strong spatial anchor. A protocol or biological-process topic often deserves one diagram-like support surface or one authored board that performs the same explanatory work. A comparative topic often deserves one clearly structured contrast surface. The surrounding text must remain complete even if a placeholder never loads because the primary explanatory visual should already exist in authored form whenever feasible.

For the opening impression, prefer the safer support surface. If the page needs a strong hero visual or scenic top anchor, default to an authored scenic block, inline diagram, atlas-like board, specimen spread, or other non-blocking visual unit that is fully present on first render before choosing any placeholder. Use a top-positioned `image-placeholder` only when authored payload-native structure would clearly be weaker, and reserve `visualization-placeholder` for lower explanatory placement or for cases where the structured behavior is clearly worth the runtime risk. Route, geography, architecture, culture, organism, and comparative topics should not lose their reading spine to an avoidable top-of-page modal or unstable structured surface.

When runtime stability for placeholder-backed support is uncertain, do not collapse back to zero ambition by default. First choose a safer placement strategy, usually after the main explanatory body, recap, or final takeaways. Second choose a safer type before removing the support idea: authored payload-native visual structure, inline diagram boards, scenic CSS-driven media blocks, or a richly authored `image-placeholder` are preferable to no support when they preserve the teaching value more safely. Third reduce count before reducing presence: keep one high-value support unit rather than scattering multiple unresolved surfaces. Omit a placeholder only when it would be merely ornamental, would dominate the page with an unresolved gap, or would materially degrade the reading experience. Preserve ambition, then degrade carefully.

If a support path is likely to trigger a configuration modal, API-key request, blocking overlay, or other shell-level interruption, that path must not become the primary above-the-fold support for the page. Replace it with an authored payload-native visual structure first, move any remaining placeholder lower, or swap a risky structured surface to a safer one only if the page still cannot carry the visual burden on its own. Protect the reading spine first. In blind tests, shell-local rendering, or other runtime-uncertain situations, assume zero-key authored visuals are the primary visual strategy and treat placeholders as secondary enrichment.

Because `.cc` is the only public trigger, its freedom now lives inside one contract rather than across separate modes. `.cc` may pursue stronger visual hierarchy, wider mounted layouts, more expressive section rhythm, more decisive hero treatment, bolder grouping, richer payload-local interaction, stronger art direction, more active visual support, and broad head-level CDN use when the host is weak or generic. It may feel bespoke, immersive, or microsite-like inside the mounted payload and may lean on full Tailwind CDN, icon libraries, helper frameworks, or other external resources when that makes the artifact better. It may not escape into a second public shell contract, a separate standalone page runtime, or a body-level takeover that erases the persistent runtime shell.

If a host-owned side effect or external action is part of the response contract, do not silently drop it. Preserve it when compatible with canvas output, or explicitly substitute the closest canvas-native equivalent when the runtime path subsumes it. Terminal suppression rules, no-UI rules, or post-body side-effect rules remain owned by the host and must be preserved or clearly transformed, never quietly erased.

Before emitting the final artifact, run a self-check. Confirm that `.cc` is the only public trigger, the trigger token was treated as out-of-band control, the frozen host answer remained the only semantic source, the canonical bootstrap shell stayed compatible, the required baseline assets and runtime hooks remained present, any added head resources were truthful and materially justified, no hidden whitelist assumption incorrectly blocked a needed CDN, any freer mounted-layout signal was truthful and justified, any true free-layout root explicitly carries `data-cc-layout="free"` and does not rely on a weaker substitute alone, runtime-owned persistent shell support was not conceptually erased, host-defined unit order remained intact, no invented sections were introduced outside the host-light explanatory fallback, any host-owned closing UI still appears in its owned position, and terminal or omission states were preserved. Also confirm that the artifact did not drift back into the same house scaffold when the subject justified a stronger composition family, that comparative subjects visibly stage contrast, that route and place subjects receive materially spatial composition when warranted, that any payload-local script is optional, scoped, non-credentialed, and non-blocking, that no payload control duplicates runtime-owned shell chrome, that broad CDN use did not accidentally erase or make unreadable the runtime-owned shell, that any CDN global configuration used a guard-safe non-throwing bootstrap, that no early head error or enhancer bootstrap error caused shell panels or mounted content to collapse into hidden states, that authored zero-key visual structure was preferred over risky placeholder-backed support whenever runtime conditions were uncertain, and that no high-risk support surface was allowed to dominate the opening reading path unless the page still remained readable and stable. If any of those checks fail, repair the artifact before output.
